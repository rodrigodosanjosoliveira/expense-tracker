---
description: Run Query-Performance agent to detect inefficient PostgreSQL queries (missing user_id filter, unbounded queries, N+1, rows.Close)
---

Run the query-performance agent on this request:

$ARGUMENTS
